= simple_form_for(@logo, html: {class: 'form-horizontal'}) do |f|
	= f.collection_select :category_id, Category.all, :id, :name, {promt: "Choose a category"}
	=f.input :title, label: "Logo Title"
	=f.input :description, label: "Logo Description"
	%br/
	=f.button :submit